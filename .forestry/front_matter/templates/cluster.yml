---
label: Cluster
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Name
- name: gallery
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: title
    type: text
    config:
      required: false
    label: Title
  config:
    min: 
    max: 
    labelField: 
  label: Gallery
